---
# =========================================================================
# Configure USG6000V2 firewalls as a full active/standby HA pair.
#
# Runs on localhost and drives the expect script `usg.exp --config`.
#
# DESIGN NOTE (USG constraint): each firewall SINGLE-HOMES per purpose.
# USG rejects the same IP on two sub-interfaces, so the crossed GE1/0/0
# cables stay plugged but are UNCONFIGURED spare/backup paths. Redundancy
# comes from the HA pair + the core-switch L2 ring (Po1):
#
#   Primary  (active):  GE1/0/1 (untrust) -> SW1, GE1/0/4 (trust) -> CS1
#   Secondary(standby): GE1/0/1 (untrust) -> SW2, GE1/0/4 (trust) -> CS2
#
# Full config (sub-interfaces, zones, OSPF, policies, heartbeat) is applied
# on the ACTIVE firewall and mirrored to STANDBY via HRP. The standby only
# needs its heartbeat link + HRP enabled locally.
#
# Order matters for HRP:
#   1. ACTIVE  gets full config (sub-interfaces, zones, OSPF, policies,
#      heartbeat link) but HRP is NOT enabled yet.
#   2. STANDBY gets sysname, heartbeat link, HRP interface + hrp enable.
#   3. ACTIVE  gets hrp enable last -> negotiation -> sync to standby.
#
# The management interface / SSH (vty+aaa) are already configured manually
# and are intentionally NOT touched here.
#
# Usage:
#   ansible-playbook -i Inventory/Connection.ini playbooks/usg_firewall_playbook.yml
# =========================================================================

- name: Configure USG6000V2 Full HA (active/standby)
  hosts: usg
  gather_facts: false
  connection: local
  vars:
    usg_exp: "{{ playbook_dir }}/../usg.exp"
    # Ansible/automation SSH management account (admin) and its reachable
    # management IPs. `Monirom` is a separate account used only for a demo
    # from the LAN (trust zone), not for automation.
    usg_mgmt_user: "admin"
    usg_ssh_ip_active: "192.168.71.100"
    usg_ssh_ip_standby: "192.168.71.101"
    active_host: "{{ groups['usg'] | map('extract', hostvars) | selectattr('usg_role', 'equalto', 'active') | map(attribute='inventory_hostname') | first }}"
    standby_host: "{{ groups['usg'] | difference([active_host]) | first }}"
    # One-time bootstrap for the STANDBY. Set to true only for the FIRST
    # deployment; afterwards the standby is in HRP standby state and rejects
    # local config changes (config now flows from active via HRP mirror).
    bootstrap_standby: false

    # NOTE: no '#' comments allowed inside these command strings — usg.exp
    # sends every non-empty line to the firewall CLI verbatim.
    #
    # active_main_cmds notes:
    #   - `dhcp enable` must precede `dhcp select interface` on GE1/0/4.100
    #     (VLAN 100 Admin pool: 192.168.10.0/24, GW 192.168.10.1, DNS 8.8.8.8).
    #   - `undo ospf mtu-enable` on the untrust sub-interfaces prevents OSPF
    #     peers sticking in ExStart (USG advertises MTU 0 vs Cisco 1500).
    #   - static default route via ISP-1 is the safety net for the default
    #     route that OSPF should install from the ISPs' ASE LSA.
    active_main_cmds: |-
      sysname {{ hostvars[active_host].usg_sysname }}
      interface {{ hostvars[active_host].hrp_interface }}
       ip address {{ hostvars[active_host].hrp_local_ip }} {{ hostvars[active_host].hrp_netmask }}
       undo shutdown
       quit
      firewall zone dmz
       add interface {{ hostvars[active_host].hrp_interface }}
       quit
      hrp interface {{ hostvars[active_host].hrp_interface }} remote {{ hostvars[active_host].hrp_remote_ip }}
      hrp mirror config enable
      dhcp enable
      dns resolve
      dns server 8.8.8.8
      dns server 1.1.1.1
       interface GigabitEthernet1/0/1.10
        vlan-type dot1q 10
        undo ip address
        ip address 10.1.1.2 255.255.255.0
        service-manage enable
        service-manage ssh permit
        service-manage ping permit
        undo ospf mtu-enable
        undo shutdown
        quit
       interface GigabitEthernet1/0/1.20
        vlan-type dot1q 20
        undo ip address
        ip address 20.1.1.2 255.255.255.0
        service-manage enable
        service-manage ssh permit
        service-manage ping permit
        undo ospf mtu-enable
        undo shutdown
        quit
      interface GigabitEthernet1/0/4.100
       vlan-type dot1q 100
       undo ip address
       ip address 192.168.10.1 255.255.255.0
       service-manage enable
       service-manage ping permit
       service-manage ssh permit
       dhcp select interface
       dhcp server dns-list 8.8.8.8
       dhcp server lease day 1
       undo shutdown
       quit
      interface GigabitEthernet1/0/4.200
       vlan-type dot1q 200
       undo ip address
       ip address 192.168.20.1 255.255.255.0
       service-manage enable
       service-manage ping permit
       undo shutdown
       quit
      interface GigabitEthernet1/0/4.300
       vlan-type dot1q 300
       undo ip address
       ip address 192.168.30.1 255.255.255.0
       service-manage enable
       service-manage ping permit
       undo shutdown
       quit
      firewall zone untrust
       add interface GigabitEthernet1/0/1.10
       add interface GigabitEthernet1/0/1.20
       quit
      firewall zone trust
       add interface GigabitEthernet1/0/4.100
       add interface GigabitEthernet1/0/4.200
       quit
      firewall zone dmz
       add interface GigabitEthernet1/0/4.300
       quit
       ospf 1 router-id 172.16.0.1
        area 0.0.0.0
         network 10.1.1.0 0.0.0.255
         network 20.1.1.0 0.0.0.255
         network 192.168.10.0 0.0.0.255
         network 192.168.20.0 0.0.0.255
         network 192.168.30.0 0.0.0.255
         quit
        quit
       ip-link check enable
       ip-link name isp1_link
        destination 10.1.1.1 interface GigabitEthernet1/0/1.10
        quit
       ip-link name isp2_link
        destination 20.1.1.1 interface GigabitEthernet1/0/1.20
        quit
       undo ip route-static 0.0.0.0 0.0.0.0 10.1.1.1
       undo ip route-static 0.0.0.0 0.0.0.0 20.1.1.1
       ip route-static 0.0.0.0 0.0.0.0 10.1.1.1 preference 60 track ip-link isp1_link
       ip route-static 0.0.0.0 0.0.0.0 20.1.1.1 preference 60 track ip-link isp2_link
       interface Tunnel1
        ip address 10.99.0.1 255.255.255.252
        tunnel-protocol gre
        source 10.1.1.2
        destination {{ ipsec_isp1_peer_ip }}
        quit
       firewall zone untrust
        add interface Tunnel1
        quit
       ip route-static 30.30.30.0 255.255.255.240 Tunnel1
        security-policy
        rule name VPN_to_Trust
         source-zone untrust
         destination-zone trust
         source-address 30.30.30.0 mask 255.255.255.240
         destination-address 192.168.10.0 mask 255.255.255.0
         destination-address 192.168.20.0 mask 255.255.255.0
         action permit
        quit
        rule name Local_to_Untrust
         source-zone local
         destination-zone untrust
         service dns
         service http
         service https
         service icmp
         service ntp
         action permit
        quit
        rule name VPN_to_DMZ
         source-zone untrust
         destination-zone dmz
         source-address 30.30.30.0 mask 255.255.255.240
         destination-address 192.168.30.0 mask 255.255.255.0
         action permit
        quit
        rule name DMZ_to_Untrust
         source-zone dmz
         destination-zone untrust
         action permit
        quit
        rule name Trust_to_Local
         source-zone trust
         destination-zone local
         service dns
         service icmp
         action permit
        quit
        rule name Untrust_to_Local_VPN
         source-zone untrust
         destination-zone local
         source-address 10.1.1.3 mask 255.255.255.255
         source-address 10.1.3.3 mask 255.255.255.255
         source-address 20.1.1.3 mask 255.255.255.255
         source-address 20.1.3.3 mask 255.255.255.255
         service gre
         service esp
         service ah
         service protocol udp destination-port 500
         service protocol udp destination-port 4500
         action permit
        quit
        rule name Untrust_to_Local_L2TP
         source-zone untrust
         destination-zone local
         service ike
         service l2tp
         action permit
        quit
        rule name Local_to_Trust
         source-zone local
         destination-zone trust
         action permit
        quit
        rule name Untrust_to_Local_ICMP
         source-zone untrust
         destination-zone local
         service icmp
         action permit
        quit
        rule name SSH_to_Local
         destination-zone local
         source-address 192.168.10.0 mask 255.255.255.0
         source-address 192.168.71.0 mask 255.255.255.0
         service https
         service ssh
         action permit
        quit
        rule name Deny_SSH_to_Local
         destination-zone local
         service https
         service ssh
         service telnet
         action deny
        quit
        rule name Block_Staff_to_Admin
         source-zone trust
         destination-zone trust
         source-address 192.168.20.0 mask 255.255.255.0
         destination-address 192.168.10.0 mask 255.255.255.0
         action deny
        quit
        rule name Admin_to_DMZ_Full
         source-zone trust
         destination-zone dmz
         source-address 192.168.10.0 mask 255.255.255.0
         destination-address 192.168.30.0 mask 255.255.255.0
         action permit
        quit
        rule name Staff_to_DMZ_WebOnly
         source-zone trust
         destination-zone dmz
         source-address 192.168.20.0 mask 255.255.255.0
         destination-address 192.168.30.0 mask 255.255.255.0
         service http
         service https
         service icmp
         action permit
        quit
        rule name Block_Staff_to_DMZ_Backend
         source-zone trust
         destination-zone dmz
         source-address 192.168.20.0 mask 255.255.255.0
         destination-address 192.168.30.0 mask 255.255.255.0
         action deny
        quit
        rule name Admin_to_Internet
         source-zone trust
         destination-zone untrust
         source-address 192.168.10.0 mask 255.255.255.0
         action permit
        quit
        rule name Staff_to_Internet_Productivity
         source-zone trust
         destination-zone untrust
         source-address 192.168.20.0 mask 255.255.255.0
         service dns
         service http
         service https
         service icmp
         action permit
        quit
        rule name Block_Staff_Other_Internet
         source-zone trust
         destination-zone untrust
         source-address 192.168.20.0 mask 255.255.255.0
         action deny
        quit
        rule name Local_to_Untrust_OSPF
         source-zone local
         destination-zone untrust
         service gre
         service ospf
         action permit
        quit
        rule name Untrust_to_Local_OSPF
         source-zone untrust
         destination-zone local
         service gre
         service ospf
         action permit
        quit
       quit
        nat-policy
         rule name Local_to_Untrust_NAT
          source-zone local
          destination-zone untrust
          action source-nat easy-ip
         quit
         rule name Trust_to_Untrust_NAT
          source-zone trust
          destination-zone untrust
          source-address 192.168.10.0 mask 255.255.255.0
          source-address 192.168.20.0 mask 255.255.255.0
          action source-nat easy-ip
         quit
         rule name DMZ_to_Untrust_NAT
          source-zone dmz
          destination-zone untrust
          source-address 192.168.30.0 mask 255.255.255.0
          action source-nat easy-ip
         quit
         rule name NO_NAT_To_Remote_VPN
          source-zone trust
          destination-zone untrust
          destination-address 30.30.30.0 mask 255.255.255.240
          action no-nat
         quit
       quit
      update schedule location-sdb weekly Sun 00:08
      update schedule ips-sdb daily 02:00
      update schedule av-sdb daily 02:30
      update schedule url-remote-query enable
      interface GigabitEthernet0/0/0
       service-manage enable
       service-manage ssh permit
       service-manage ping permit
       service-manage https permit
       undo shutdown
       quit
      aaa
       manager-user Monirom
        password cipher proxy@m@n!r0m
        service-type ssh terminal
        level 15
        quit
       bind manager-user Monirom role system-admin
       quit
      stelnet server enable
      undo telnet server enable
      undo telnet ipv6 server enable
      undo ssh server compatible-ssh1x enable
      ssh authentication-type default password
      ssh user Monirom authentication-type password
      ssh user Monirom service-type all
      user-interface vty 0 4
       authentication-mode aaa
       protocol inbound ssh
       quit

    active_hrp_cmds: |-
      hrp enable

    standby_cmds: |-
      sysname {{ hostvars[standby_host].usg_sysname }}
      interface {{ hostvars[standby_host].hrp_interface }}
       ip address {{ hostvars[standby_host].hrp_local_ip }} {{ hostvars[standby_host].hrp_netmask }}
       undo shutdown
       quit
      firewall zone dmz
       add interface {{ hostvars[standby_host].hrp_interface }}
       quit
      hrp interface {{ hostvars[standby_host].hrp_interface }} remote {{ hostvars[standby_host].hrp_remote_ip }}
      hrp enable
      hrp switch standby

     # =========================================================================
     # IPsec site-to-site VPN (dual ISP, IKEv2, certificate auth)
     #
     #   Active/standby tunnels:
     #     Tunnel 1 (primary):  10.1.1.2  <-> 10.1.1.3  (ISP1)
     #     Tunnel 2 (backup):   20.1.1.2  <-> 20.1.1.3  (ISP2)
     #
     # Both tunnels expose the same remote LAN (30.30.30.0/28). The USG uses
     # the higher-priority tunnel first; if it fails, the second takes over.
     #
     # NOTE: certificate generation (pki rsa + self-signed) is included here.
     # After the first run, export the local cert from each USG and import the
     # peer's cert on the other side manually. The peer cert is trusted via
     # peer-id fqdn {{ ipsec_remote_cn }}.
     # =========================================================================
    ipsec_remote_subnet: 30.30.30.0 0.0.0.15
    ipsec_remote_cn: Monirom-USG6000V
    ipsec_isp1_peer_ip: 10.1.3.3
    ipsec_isp2_peer_ip: 20.1.3.3
    ipsec_local_cn: USG6000V2-Primary

    active_ipsec_cmds: |-
       ike proposal 1
        encryption-algorithm aes-256
        authentication-algorithm sha2-256
        authentication-method pre-share
        integrity-algorithm hmac-sha2-256
        dh group14
        sa duration 86400
        quit
        ike peer peer-vpn1
         version 1
         ike-proposal 1
         pre-shared-key simple Monirom123
         remote-address {{ ipsec_isp1_peer_ip }}
         quit
        ipsec proposal prop-vpn
         esp authentication-algorithm sha2-256
         esp encryption-algorithm aes-256
         quit
        acl number 3001
         rule 5 permit gre source 10.1.1.2 0 destination {{ ipsec_isp1_peer_ip }} 0
         rule 10 permit gre source {{ ipsec_isp1_peer_ip }} 0 destination 10.1.1.2 0
         quit
        ipsec policy policy-gre 1 isakmp
         security acl 3001
         ike-peer peer-vpn1
         proposal prop-vpn
         pfs dh-group14
         quit
        # Bind GRE IPsec policy to the primary WAN interface (safe since it only encrypts GRE)
        interface GigabitEthernet1/0/1.10
         ipsec policy policy-gre
         quit
        interface GigabitEthernet1/0/1.20
         undo ipsec policy
         quit

  tasks:
    # Push full configuration (L3 sub-interfaces, zones, OSPF, IP-Link, routes,
    # security policies, SSH management, Monirom admin account, IPsec)
    # in a single atomic SSH session to avoid connection collisions and write-locks.
    - name: Push complete configuration to ACTIVE firewall
      ansible.builtin.shell: "{{ usg_exp }} --config"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
        USG_CMDS: "{{ active_main_cmds }}\n{{ active_ipsec_cmds }}"
      register: active_main_result
      changed_when: true
      run_once: true

    - name: Show ACTIVE config output
      ansible.builtin.debug:
        var: active_main_result.stdout_lines
      run_once: true

    # Bootstraps the STANDBY firewall with its sysname, heartbeat IP, and
    # HRP channel. This is a one-time step (guarded by `bootstrap_standby`).
    # After the first run the standby is in HRP standby state and rejects
    # local config changes — all config flows from the active via HRP mirror.
    - name: Push heartbeat + HRP to STANDBY firewall
      ansible.builtin.shell: "{{ usg_exp }} --config"
      environment:
        USG_HOST: "{{ usg_ssh_ip_standby }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[standby_host].usg_password }}"
        USG_CMDS: "{{ standby_cmds }}"
      register: standby_result
      changed_when: true
      when: bootstrap_standby
      run_once: true

    - name: Show STANDBY config output
      ansible.builtin.debug:
        var: standby_result.stdout_lines
      when: bootstrap_standby
      run_once: true

    - name: Wait for firewall to finish flash commit and VTY release
      ansible.builtin.pause:
        seconds: 5
      run_once: true

    - name: Verify HRP state on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display hrp state"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: hrp_state
      changed_when: false
      run_once: true

    - name: Show HRP state
      ansible.builtin.debug:
        var: hrp_state.stdout_lines
      run_once: true

    - name: Verify HRP state on STANDBY
      ansible.builtin.shell: "{{ usg_exp }} display hrp state"
      environment:
        USG_HOST: "{{ usg_ssh_ip_standby }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[standby_host].usg_password }}"
      register: hrp_state_stdby
      changed_when: false
      run_once: true

    - name: Show STANDBY HRP state
      ansible.builtin.debug:
        var: hrp_state_stdby.stdout_lines
      run_once: true

    - name: Verify OSPF neighbors on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display ospf peer"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: ospf_peer
      changed_when: false
      run_once: true

    - name: Show OSPF peers
      ansible.builtin.debug:
        var: ospf_peer.stdout_lines
      run_once: true

    - name: Verify DHCP pool for VLAN 100 on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display ip pool interface GigabitEthernet1/0/4.100 used"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: dhcp_pool
      changed_when: false
      run_once: true

    - name: Show DHCP pool status
      ansible.builtin.debug:
        var: dhcp_pool.stdout_lines
      run_once: true

    # =========================================================================
    # ACL & Security Policy Verification
    # =========================================================================
    - name: Verify ACL 3000 configuration on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display acl 3000"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: acl_result
      changed_when: false
      run_once: true

    - name: Show ACL 3000 configuration
      ansible.builtin.debug:
        var: acl_result.stdout_lines
      run_once: true

    - name: Verify Security Policy rules on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display security-policy rule all"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: sec_policy_result
      changed_when: false
      run_once: true

    - name: Show Security Policy rules
      ansible.builtin.debug:
        var: sec_policy_result.stdout_lines
      run_once: true

    # =========================================================================
    # IPsec VPN Verification
    # =========================================================================
    - name: Verify IKE proposal configuration on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display ike proposal"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: ike_proposal_result
      changed_when: false
      run_once: true

    - name: Show IKE proposal configuration
      ansible.builtin.debug:
        var: ike_proposal_result.stdout_lines
      run_once: true

    - name: Verify IKE peer configuration on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display ike peer"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: ike_peer_result
      changed_when: false
      run_once: true

    - name: Show IKE peer configuration
      ansible.builtin.debug:
        var: ike_peer_result.stdout_lines
      run_once: true

    - name: Verify IPsec proposal configuration on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display ipsec proposal"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: ipsec_proposal_result
      changed_when: false
      run_once: true

    - name: Show IPsec proposal configuration
      ansible.builtin.debug:
        var: ipsec_proposal_result.stdout_lines
      run_once: true

    - name: Verify IPsec policy configuration on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display ipsec policy"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: ipsec_policy_result
      changed_when: false
      run_once: true

    - name: Show IPsec policy configuration
      ansible.builtin.debug:
        var: ipsec_policy_result.stdout_lines
      run_once: true

    - name: Display current IPsec and IKE running config on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display current-configuration configuration ipsec"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: cur_ipsec_cfg
      changed_when: false
      run_once: true

    - name: Show current IPsec running config
      ansible.builtin.debug:
        var: cur_ipsec_cfg.stdout_lines
      run_once: true

    - name: Check IKE SA status on ACTIVE (shows negotiated Phase 1 tunnel)
      ansible.builtin.shell: "{{ usg_exp }} display ike sa"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: ike_sa_result
      changed_when: false
      run_once: true

    - name: Show IKE SA status
      ansible.builtin.debug:
        var: ike_sa_result.stdout_lines
      run_once: true

    - name: Check IPsec SA status on ACTIVE (shows negotiated Phase 2 tunnel)
      ansible.builtin.shell: "{{ usg_exp }} display ipsec sa"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: ipsec_sa_result
      changed_when: false
      run_once: true

    - name: Show IPsec SA status
      ansible.builtin.debug:
        var: ipsec_sa_result.stdout_lines
      run_once: true

    # =========================================================================
    # IP-Link Health Check & Routing Verification
    # =========================================================================
    - name: Verify IP-Link health check status on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display ip-link"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: ip_link_result
      changed_when: false
      run_once: true

    - name: Show IP-Link health check status
      ansible.builtin.debug:
        var: ip_link_result.stdout_lines
      run_once: true

    - name: Verify Active Default Route on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display ip routing-table 0.0.0.0"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: default_route_result
      changed_when: false
      run_once: true

    - name: Show Active Default Route
      ansible.builtin.debug:
        var: default_route_result.stdout_lines
      run_once: true

    # =========================================================================
    # Secure SSH Management & Administrator Verification
    # =========================================================================
    - name: Verify Monirom Administrator Account on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display manager-user Monirom"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: admin_user_result
      changed_when: false
      run_once: true

    - name: Show Monirom Administrator Account details
      ansible.builtin.debug:
        var: admin_user_result.stdout_lines
      run_once: true

    - name: Verify STelnet and SSH Server Status on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display stelnet server status"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: stelnet_status_result
      changed_when: false
      run_once: true

    - name: Show STelnet Server Status
      ansible.builtin.debug:
        var: stelnet_status_result.stdout_lines
      run_once: true

    - name: Verify ACL 2000 Management Restriction on ACTIVE
      ansible.builtin.shell: "{{ usg_exp }} display acl 2000"
      environment:
        USG_HOST: "{{ usg_ssh_ip_active }}"
        USG_USER: "{{ usg_mgmt_user }}"
        USG_PASS: "{{ hostvars[active_host].usg_password }}"
      register: acl2000_result
      changed_when: false
      run_once: true

    - name: Show ACL 2000 Management Restriction
      ansible.builtin.debug:
        var: acl2000_result.stdout_lines
      run_once: true